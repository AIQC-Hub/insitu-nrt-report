---
title: "Practical salinity"
description: |
  Basic analysis of the salinity data from Baltic Sea in situ NRT observations.
output:
  distill::distill_article:
    toc: true
    toc_depth: 2
---

```{r, init, include = FALSE}
knitr::opts_chunk$set(echo=FALSE)

# Common functions
r_func_path <- "./_func"
r_funcs <- c("libraries.Rmd", "common.Rmd", "common_bo.Rmd", "var.Rmd")

# Variables
var <- "psal"
var_name <- "Practical salinity"
var_label <- "Psal"
```

```{r, import, include = FALSE, child=file.path(r_func_path, r_funcs)}
```

## 1. Basic Information
```{r, basic_info, results = 'asis'}
res <- knitr::knit_expand(t_v_basic_info)
cat(res, sep = '\n')
```

---

## 2. Summary statistics
```{r, summary_stats, results = 'asis'}
src <- knitr::knit_expand(t_v_summary_stats, df=df_name)
res <- knitr::knit_child(text = src, quiet = TRUE)
cat(res, sep = '\n')
```

---

## 3. Distributions over time and location
```{r, distributions, results = 'asis'}
src <- knitr::knit_expand(t_v_distributions)
res <- knitr::knit_child(text = src, quiet = TRUE)
cat(res, sep = '\n')
```

---
